const express = require("express") // importando o express
const createUser = express.Router() // objeto do roteador (cada um é uma rota de interação com a tb do banco de dados)

// bla
module.exports = createUser